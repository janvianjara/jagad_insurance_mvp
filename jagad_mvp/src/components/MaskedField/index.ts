/**
 * A labelled identifier the staff UI may only see the tail of. The constitution's
 * Aadhaar rule, made a component so no screen has to remember it.
 */
export { MaskedField } from './MaskedField'
export type { MaskedFieldProps } from './MaskedField'
